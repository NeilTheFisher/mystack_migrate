{
  "name": "mystack_migrate",
  "private": true,
  "scripts": {
    "build": "bun build --compile --minify --outfile ./dist/migrate_neil_stack migrate_neil_stack.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
