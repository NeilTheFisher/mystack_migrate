{
  "name": "mystack_migrate",
  "private": true,
  "scripts": {
    "build": "bun build --target=bun --sourcemap=inline --outdir dist migrate_neil_stack.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
